extends layout

block content
  h1= title

  ul
    each category in category_list
      li
        a(href=category.url) #{category.name}
  
  p
    a(href="/inventory/categories/create") Create category
  p
    a(href="/") Back home